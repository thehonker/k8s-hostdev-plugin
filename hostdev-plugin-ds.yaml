---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: hostdev-device-plugin
  namespace: kube-system
spec:
  selector:
    matchLabels:
      name: hostdev-device-plugin-ds
  template:
    metadata:
      labels:
        name: hostdev-device-plugin-ds
    spec:
      securityContext:
        privileged: true
        seccompProfile:
          type: Unconfined
      hostNetwork: true
      imagePullPolicy: Always
      containers:
      - name: hostdev
        image: ghcr.io/thehonker/k8s-hostdev-plugin:latest
        # passthrough an amd gpu at 0000:05:00.0
        # args: ["--devs", "/dev/mem:r,/dev/fuse:rwm,/dev/kfd:rwm,/dev/dri/by-path/pci-0000:05:00.0-card:rwm,/dev/dri/by-path/pci-0000:05:00.0-render:rwm"]
        args: ["--devs", "/dev/mem:r,/dev/fuse:rwm"]
        volumeMounts:
        - name: device-plugin
          mountPath: /var/lib/kubelet/device-plugins
        - name: dev
          mountPath: /dev
      volumes:
      - name: device-plugin
        hostPath:
          path: /var/lib/kubelet/device-plugins
      - name: dev
        hostPath:
          path: /dev
