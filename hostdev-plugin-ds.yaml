apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: hostdev-device-plugin
  namespace: kube-system
spec:
  selector:
    matchLabels:
      name: hostdev-device-plugin-ds
  template:
    metadata:
      labels:
        name: hostdev-device-plugin-ds
    spec:
      hostNetwork: true
      containers:
      - name: hostdev
        image: ghcr.io/thehonker/k8s-hostdev-plugin:latest
        args: ["--devs", "/dev/mem:r,/dev/fuse:rwm,/dev/kfd:rwm,/dev/dri:rmw"]
        securityContext:
          privileged: true
        volumeMounts:
        - name: device-plugin
          mountPath: /var/lib/kubelet/device-plugins
        - name: dev
          mountPath: /dev
      volumes:
      - name: device-plugin
        hostPath:
          path: /var/lib/kubelet/device-plugins
      - name: dev
        hostPath:
          path: /dev
